## Vagrant for vyos-build

This is the vagrant file for build 
[vyos-build](https://github.com/vyos/vyos-build/commits/current)

## Setup

```
vagrant up
vagrant ssh
```

## Usage

```
cd vyos-build
sudo ./configure
sudo make iso
```
